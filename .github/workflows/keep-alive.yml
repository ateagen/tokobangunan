name: Supabase Keep Alive

on:
  schedule:
    - cron: "0 0 */3 * *"  # tiap 3 hari pukul 00:00 UTC
  workflow_dispatch:      # bisa dijalankan manual dari GitHub Actions

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: curl -s https://ateagen.github.io/tokobangunan/
